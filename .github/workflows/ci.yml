name: CI

on: push

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Lint
        uses: pwsh
        run: ./.github/Assert-Linting
      - name: Test
        uses: pwsh
        run: ./.github/Assert-Tests
